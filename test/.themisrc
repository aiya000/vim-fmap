call themis#helper('command').with(themis#helper('assert'))
call themis#option('runtimepath', ['..'])

function! InitializeVariables() abort
    let g:fmap_mappings = [
      \ { 'strokes': [','], 'target': '、' },
      \ { 'strokes': ['.'], 'target': '。' },
    \ ]
endfunction
